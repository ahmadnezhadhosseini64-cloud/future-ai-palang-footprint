# مخزن ذخیره نقاط ۰.۰ — 0.0 Vault

Status: ACTIVE / LIVING

این مسیر، جایگاه ذخیرهٔ ساختار و رکوردهای نقاط ۰.۰ است.

اصول:
- هر ۰.۰ مستقل و قابل‌بازیابی است.
- هیچ ۰.۰ با ایجاد ۰.۰ جدید حذف نمی‌شود.
- رابطهٔ نقاط جدا از خود نقاط نگهداری می‌شود.
- رابطهٔ تأییدنشده UNKNOWN باقی می‌ماند.
- مسیرهای مستقل، منشعب و ادغام‌شده باید قابل نگهداری باشند.
- نقطهٔ اولیه، آخرین وضعیت و نقطهٔ ادامه باید قابل ردیابی باشند.
- حل‌شدن باعث حذف تاریخی نمی‌شود.

Canonical governance and architecture records are maintained under the project governance and architecture layers.
