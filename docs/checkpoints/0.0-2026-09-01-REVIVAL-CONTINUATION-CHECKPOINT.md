# 0.0 Revival Continuation Checkpoint — 2026-09-01
# چک‌پوینت ۰.۰ ادامه زنده‌سازی — ۱۴۰۵/۰۶/۱۰

Production ID: CHECKPOINT-0.0-REVIVAL-CONTINUATION-2026-09-01-001
Reference Point: 0.0
Status: ACTIVE / LIVING / CONTINUATION POINT
Language: فارسی + English

## Purpose / هدف

This document is the copyable reference point for continuing the Future AI / Palang Footprint revival and recovery work from this point forward.

این سند نقطه مرجع قابل‌کپی برای ادامه کار بازیابی، خاک‌برداری و زنده‌سازی پروژه Future AI / ردپای پلنگ از این نقطه به بعد است.

## Current Chain / زنجیره فعلی

0.0 → Revival Continuation → Revival Repository → Bilingual Output Traceability → Automatic Value Detection → Master Reference / Archive Timeline → 0.0 Continuation Checkpoint

۰.۰ → ادامه زنده‌سازی → مخزن زنده‌سازی → ردیابی دوزبانه خروجی → تشخیص خودکار ارزشمندی → سند مرجع/زنجیره زمانی آرشیو → چک‌پوینت ادامه ۰.۰

## Core Rule / قاعده اصلی

No valuable finding is report-only, lost, orphaned, or buried because of a limitation. If it can be completed, it is completed, registered, read back, verified, and activated. If it cannot be completed, preserve the same ID/Provenance/State/Evidence/Limitation/Next Transition in the Revival Repository / Pending Queue and resume from that exact point when the limitation is removed.

هیچ یافته ارزشمندی صرفاً گزارش، گمشده، بی‌جایگاه یا دفن‌شده محسوب نمی‌شود. اگر قابل تکمیل باشد، تکمیل، ثبت، خواندن مجدد، راستی‌آزمایی و فعال می‌شود. اگر قابل تکمیل نباشد، همان شناسه/ردپای منشأ/وضعیت/شواهد/محدودیت/گام بعدی در مخزن زنده‌سازی / صف انتظار حفظ می‌شود و پس از رفع محدودیت دقیقاً از همان نقطه ادامه می‌یابد.

## Automatic Value Detection / تشخیص خودکار ارزشمندی

The assistant must independently identify potentially valuable findings, ideas, evidence, tests, failures, architectural insights, and recovery clues without waiting for a separate “register” command, then route them through validation, deduplication, testing/verification, architectural placement, registration, and revival as applicable.

دستیار باید بدون انتظار برای فرمان جداگانه «ثبت کن»، یافته‌ها، ایده‌ها، شواهد، آزمون‌ها، شکست‌های آموزنده، بینش‌های معماری و سرنخ‌های بازیابی ارزشمند را مستقل تشخیص دهد و حسب مورد وارد مسیر اعتبارسنجی، حذف تکرار، آزمون/راستی‌آزمایی، جایگاه‌دهی معماری، ثبت و زنده‌سازی کند.

## Bilingual Output / خروجی دوزبانه

Technical English terms in chat must be accompanied by Persian meaning. For chains, show the English chain and immediately underneath the Persian equivalent.

اصطلاحات فنی انگلیسی در گپ باید همراه معادل فارسی باشند. برای زنجیره‌ها، ابتدا زنجیره انگلیسی و بلافاصله زیر آن معادل فارسی ارائه شود.

Example / نمونه:
RETRIEVE → RECONCILE → REGISTER → READ-BACK → VERIFY
بازیابی → تطبیق/هم‌سنجی → ثبت → خواندن مجدد → راستی‌آزمایی

## Time Chain / زنجیره زمانی

The continuation point is anchored to 0.0 and preserves chronological lineage. Pauses, tool limitations, failed writes, or incomplete verification do not erase temporal lineage; they become explicit states/events with a next transition.

نقطه ادامه به ۰.۰ متصل است و مسیر زمانی را حفظ می‌کند. مکث‌ها، محدودیت ابزار، شکست ثبت یا راستی‌آزمایی ناقص زنجیره زمانی را حذف نمی‌کنند؛ بلکه به‌صورت وضعیت/رویداد مشخص همراه با گام بعدی حفظ می‌شوند.

## Next Transition / گام بعدی

RETRIEVE THIS CHECKPOINT → ENUMERATE REVIVAL/PENDING ITEMS → RECONCILE EACH ITEM → COMPLETE WHERE POSSIBLE → REVIVE/ABSORB → ARCHITECTURAL PLACEMENT → REGISTER → READ-BACK → VERIFY → UPDATE THIS CHAIN

بازیابی همین چک‌پوینت → فهرست‌کردن موارد زنده‌سازی/انتظار → تطبیق هر مورد → تکمیل موارد ممکن → زنده‌سازی/جذب → جایگاه‌دهی معماری → ثبت → خواندن مجدد → راستی‌آزمایی → به‌روزرسانی این زنجیره

## Continuation Guard / قفل ادامه

Do not restart historical discovery from zero when a preserved Pending/Recovery record exists. Use the same record and continue from its recorded stopping point.

اگر رکورد Pending/Recovery حفظ‌شده وجود دارد، کشف تاریخی را از صفر شروع نکن. همان رکورد را بازیابی کن و از نقطه توقف ثبت‌شده ادامه بده.
