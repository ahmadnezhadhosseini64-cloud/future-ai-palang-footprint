# REG-2026-09-04-0.0-YOUTUBE-CONTEXT-SWITCH-001

## 0.0 Context Boundary
- Date: 2026-09-04
- Reference: `0.0`
- Status: `ACTIVE / LIVING`
- Type: `Context Boundary / Domain Switch`
- Owner: Ahmad Nezhadhosseini
- Project lineage: Future AI / Palang Footprint

## Decision
The current point is explicitly established as a clean `0.0` boundary for leaving the Future AI / Palang Footprint execution template and entering the YouTube working context.

This does **not** mean that every later continuation of `0.0` is automatically a YouTube task. The `0.0` boundary is preserved independently from the active conversation topic.

## Operating Rule
When the user later says `شروع کن` after entering the YouTube context:
1. Resume from this `0.0` boundary.
2. Preserve the boundary and do not lose it during ordinary conversation or questions.
3. Do not infer the subject of the next execution merely from the existence of `0.0`.
4. Use the YouTube context only when the user has explicitly entered/selected that context.
5. Casual questions, discussion, or temporary conversation do not overwrite, consume, or redefine `0.0`.
6. A new execution start must be explicitly recognized as `START FROM 0.0` only when the user gives the start command.

## Separation Rule
`0.0` = preserved recovery/start boundary.
`YouTube Context` = active working domain.
`Casual Chat / Question` = temporary detour; it must return to the preserved boundary/context and must not redefine it.

## No-Fake-Closure
This record registers the boundary and operating rule only. It does not claim provider-level ChatGPT Persistent Memory verification.

## English Key
**0.0 = preserved boundary, not automatic topic inference.**
**YouTube Context = working domain.**
**Casual Chat = temporary detour; return without losing 0.0.**
